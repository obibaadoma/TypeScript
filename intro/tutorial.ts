let id: number = 5;
let company: string = 'Daa Nkwa';
let isPublished: boolean = true;

let arrayNumbers: number[] = [1, 2, 3, 4, 5];
let arrayStrings: string[] = ['Hello', 'World'];

const concatenateValues = (a: string, b: string): string => {
  return a + b;
};

console.log(concatenateValues("Hello", "world"));